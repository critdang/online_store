import React, { Component } from 'react';
import Typography from '@mui/material/Typography';

export default class nomatch extends Component {
  render() {
    return <Typography sx={{ alignItems: 'center' }}>No Match</Typography>;
  }
}
